<h1>Result</h1>

<div>
  <p>
    <%= @message.gsub(/\b#{Regexp.escape(params[:word])}\b/i) { |match| "<strong>#{match.upcase}</strong>" }.html_safe %>
  </p>

<%= link_to "Play again", new_path, class: "text-decoration-none" %>
</div>
